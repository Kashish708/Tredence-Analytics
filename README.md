
# Self-Pruning Neural Network

## Overview

This project implements a neural network that learns to prune itself during training using learnable gates and sparsity regularization.

## Key Features

* Learnable pruning mechanism
* L1-based sparsity regularization
* Dynamic architecture adaptation

## Results

* Demonstrates trade-off between sparsity and accuracy
* Produces interpretable gate distributions
